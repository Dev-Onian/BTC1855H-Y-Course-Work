#' this is a test script
#' The single quote after the hashtag makes this a command line
#' And makes it easy to add more comments
#' Until you delete the single quote
# Then it won't work

# see, it didn't add a comment
#' Anyways, we'll do some code and then push it

myvector <- seq(100, 1, by=-7)

myavg <- mean(myvector)
myavg

mysum <- sum(myvector)
mysum

mysd <- sd(myvector)
mysd
